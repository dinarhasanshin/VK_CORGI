<Window x:Class="VK_CORGI.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view ="clr-namespace:VK_CORGI.ViewModels"
        xmlns:local="clr-namespace:VK_CORGI"
        Title="Window" 
        Height="450" Width="800">
    <Window.DataContext>
        <view:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="6*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <Button Content="Home" Command="{Binding PageSelectedCommand}" CommandParameter="Home" Margin="1,1,0,1"></Button>
            <Button Content="View" Command="{Binding PageSelectedCommand}" CommandParameter="View" Margin="1,0,0,1"></Button>
        </StackPanel>
        <ContentControl Grid.Column="1" Content="{Binding SelectedViewModel}"></ContentControl>
    </Grid>
</Window>
